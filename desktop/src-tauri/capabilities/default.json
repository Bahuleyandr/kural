{
  "$schema": "https://schema.tauri.app/core-plugin-permission/2",
  "identifier": "default",
  "description": "Kural default capabilities — IPC for backend discovery and the dictation widget",
  "windows": ["main", "dictation"],
  "permissions": [
    "core:default"
  ]
}
