# Kural Desktop

Tauri cross-platform desktop app wrapping the Kural web UI.

**Status: Phase 2 — not yet implemented.**

Will ship Windows, macOS, and Linux binaries that bundle the frontend and auto-start the backend.

## Planned

```bash
cargo tauri dev     # development
cargo tauri build   # production binary
```
