# Kural Frontend

Next.js web UI for Kural TTS. Text → audio download in one page. No login, no cloud.

## Development

```bash
npm install
npm run dev
```

Open http://localhost:3000.

## Environment

```
NEXT_PUBLIC_API_URL=http://localhost:8000
```
