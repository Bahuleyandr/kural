# Optional Chatterbox runtime dependencies for voice cloning.
#
# chatterbox-tts 0.1.7 declares numpy<2 on Python <3.13, while kokoro-onnx
# requires numpy>=2. Install these runtime dependencies, then install
# chatterbox-tts itself with --no-deps so Kokoro and Chatterbox can share the
# same Python environment.
librosa==0.11.0
s3tokenizer
transformers==5.2.0
diffusers==0.29.0
resemble-perth>=1.0.0
conformer==0.3.2
safetensors==0.5.3
spacy-pkuseg
pykakasi==2.3.0
pyloudnorm
omegaconf
