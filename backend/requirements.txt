fastapi>=0.115.0,<1.0
uvicorn[standard]>=0.32.0,<1.0
pydantic>=2.9.0,<3.0
pydantic-settings>=2.5.0,<3.0
soundfile>=0.12.1
numpy>=2.0.2
scipy>=1.13.0
python-multipart>=0.0.9
slowapi>=0.1.9,<1.0
defusedxml>=0.7.1,<1.0
# Lightweight kokoro TTS via ONNX runtime (no PyTorch dependency).
# Pinned to a tested release; bump deliberately and run the integration test
# (KURAL_RUN_INTEGRATION=1 pytest tests/test_engine_integration.py).
kokoro-onnx==0.5.0
# Supertonic is opt-in — pip-installing it pins numpy<2 which conflicts with
# kokoro-onnx. See requirements-supertonic.txt for the install recipe.
